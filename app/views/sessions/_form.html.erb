<%= form_for :session, url: session_path do |f| %>

  <%= f.label :email, "Email:" %>
  <%= f.email_field :email %>
  <%= f.label :password, "Password:" %>
  <%= f.password_field :password %>

  <%= f.submit "Log in" %>
<% end %>
